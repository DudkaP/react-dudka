import React from 'react';

export const Posts = ({post}) => {
    // const {id, title} = post;
    console.log(post)
    // console.log(title)

    return (
        <div>
            {/*{post} ---*/}
        </div>
    );
};